const { test, expect } = require('@jest/globals')
const script = require('../client/script')

describe("getListings", () => {

    test("has fetch been called", () => {
        expect()
    })

    test("has values been assigned to variables", () => {
        expect()
    })

})
    